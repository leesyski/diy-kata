const numberToReversedDigits = (number) => {
  return number.toString().split("").map(Number).reverse();


  


  //let array = array.of(number);



  //return array;
};
// number to string then split it then number map then reverse

  
    //let output = [];
  //let sNumber = numberToReversedDigits.toString();

//for (let i = 0, len = sNumber.lenght; i < len; i += 1) {
    //output.push(+sNumber.charAt(i));

//return output
//}};
   

//const numberSplit = Array.of(number);
    
//for (let i = 0, len = numberSplit.lenght; i < len; i += 1) {
    //numberSplit.push(+sNumber.charAt(i));

//}

//return numberSplit;

//}

//const number1 = number.toString();
    //const number2 = number1.split( );

module.exports = numberToReversedDigits;

const joinNames = (namesObj) => {
    
    const justnames = namesObj.map(name => name.name)

        
    
    return justnames
    
    
//need to add & full stop the to the array
//get the number of values in array
//add .  with .push
//with the number in array -1 to place & sign

// look at string.replace 
    


    //return justnames.toString('&//');    
    //const  namesToString = test.toString()
    // 'namesObj.map returns the names from object but with the keyvalue pairs ie: "name: Bart" "name:.... etc"
    // 'name => name.name is a function that 
 
   
};


module.exports = joinNames;


// 'Bart, Lisa & Maggie'.